// Runtime configuration — overwritten by docker-entrypoint.sh in production.
// For local dev, the Vite proxy handles /api → backend, so no override needed.
// window.__WORKFORCE_TWIN_API_BASE__ = '/api/v1/workforce-twin';
