apiVersion: v1
kind: Service
metadata:
  name: {{ include "etter-backend.fullname" . }}
  labels:
    {{- include "etter-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "etter-backend.selectorLabels" . | nindent 4 }}
