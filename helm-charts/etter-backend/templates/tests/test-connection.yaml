apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "etter-backend.fullname" . }}-test-connection"
  labels:
    {{- include "etter-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "etter-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
